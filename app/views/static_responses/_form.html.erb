<%= form_for(@static_response) do |f| %>
  <% if @static_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@static_response.errors.count, "error") %> prohibited this static_response from being saved:</h2>

      <ul>
      <% @static_response.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
